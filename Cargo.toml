[package]
name = "stretch"
version = "0.1.0"
authors = ["Emil Sjölander <emil@visly.app>"]
edition = "2018"

[dependencies]

[dev-dependencies]
criterion = "0.2"

[profile.release]
lto = true
panic = 'abort'

[lib]
crate-type = ["dylib"]

[[bench]]
name = "generated"
harness = false
